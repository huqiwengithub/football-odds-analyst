# Football Odds Analyst v3.0 — MBI Framework + 投注建议判断逻辑

> 供专家评审。文档覆盖：双技能管道架构、MBI 六大模块、12+1 步分析流程、概率合成、6D 评分、混合过关组合规则。

---

## 1. 整体架构

```
用户输入「分析 X 日比赛」
         │
         ▼
┌──────────────────────────────────────────────────┐
│ football-odds-analyst v3.0 (分析引擎)              │
│                                                    │
│ Step 0: 检查工作目录 {date}_deep.json               │
│   ├─ 存在 → 直接加载                                │
│   └─ 不存在 → 自动调用 500com-football-scraper       │
│                                                    │
│ Step 1-11: 12 步分析 → MBI → 概率合成 → 过关组合     │
│                                                    │
│ 输出: HTML 分析报告 + 投注方案                       │
└──────────────────────────────────────────────────┘
                    │ 调用
                    ▼
┌──────────────────────────────────────────────────┐
│ 500com-football-scraper v2.0 (数据管道)            │
│                                                    │
│ 每场比赛抓取 6 个 500.com 深度分析页:                │
│   ouzhi: 30 家博彩公司 SPF 初盘→即时 + 凯利 + 离散   │
│   yazhi: 16 家公司 AH 初盘→即时 + 水位 + 时间戳       │
│   rangqiu: RQSPF 初盘→即时                          │
│   daxiao: OU 初盘→即时 + 盘口升降                    │
│   shuju: H2H、近期战绩、FIFA 排名、预计首发           │
│   touzhu: 必发交易所成交量、庄家盈亏、冷热指数        │
│                                                    │
│ 输出: 标准化 JSON → {date}_deep.json (12h 缓存)     │
└──────────────────────────────────────────────────┘
```

---

## 2. MBI (Multi-Bookmaker Intelligence) 框架

### 2.0 设计理念

v2.9 时代仅以 Pinnacle（平博）单一机构作为「尖峰基准」。Pinnacle 接受赢家玩家、利润率极薄(~2%)，确实是全球最敏锐的博彩定价者。但单一数据源有三个盲区：

| 盲区 | Pinnacle 局限 | MBI 解决方案 |
|:---|:---|:---|
| 亚洲资金流 | Pinnacle 主要服务欧美客户 | 澳门/皇冠/利记/12bet — 亚洲真实资金风向标 |
| 机构分歧度 | Pinnacle 一条线看不到全貌 | 30 家离散值 → 量化市场共识强度 |
| 真实成交量 | 赔率≠资金 | 必发交易所实时成交额、庄家盈亏敞口 |

### 2.1 博彩公司三层分类

30 家博彩公司按业务模式和信号权重分三层：

| 层级 | 权重 | 成员 | 信号特征 |
|:---|:---:|:---|:---|
| **Sharp 层** | 0.55 | Pinnacle、bet365、IBC(沙巴/SBO) | 接受赢家、利润率 <3%、领先价格发现 |
| **Asian 层** | 0.25 | 澳门、皇冠、利记、易胜博、12bet、18bet | 区域资金流、水位敏感、受宏观政策影响 |
| **Retail 层** | 0.20 | 威廉希尔、立博、Interwetten、必发(exchange)、伟德、Bwin | 公众情绪、娱乐性投注量、必发揭示真实资金 |

### 2.2 模块一：Sharp Consensus Score (SCS)

**目的**：不再问「Pinnacle 怎么看」，而是「30 家机构加权投票后怎么看」。

```
对每个结果（主胜/平局/客胜），遍历全部 30 家博彩公司:

  SCS_outcome = Σ(tier_weight_i × direction_i) / Σ(tier_weight_i)

  direction_i 判定:
    即时赔率 < 初盘赔率 → +1（赔率收紧 = 信心增强）
    即时赔率 > 初盘赔率 → −1（赔率走宽 = 信心减弱）
    |变动| < 2% → 0（噪音）

最终 SCS = SCS_home × 0.6 + SCS_draw × 0.2 + SCS_away × 0.2
  (主胜权重更高，因为多数比赛有明确热门方)
```

**阈值**：
- SCS ≥ 0.70 → Strong consensus → 概率校正 +10%
- SCS 0.40–0.70 → Moderate → 不调整
- SCS < 0.40 → Weak consensus → 概率校正 −5%

### 2.3 模块二：Dispersion Risk Index (DRI)

**目的**：量化机构间分歧。30 家如果各说各话，任何单一定价都不可靠。

```
DRI = normalized(
  500.com 离散值_主胜 × 0.5 +
  500.com 离散值_平局 × 0.3 +
  500.com 离散值_客胜 × 0.2
)
归一化: ÷ 100（500.com 离散值范围 0–400+）
```

**阈值**：
- DRI ≤ 15 → 高度收敛 → 置信 × 1.05
- DRI 15–40 → 正常 → 不调整
- DRI 40–70 → 高分歧 → 置信 × 0.85，⚠️ 警告
- DRI > 70 → 极端分歧 → 置信 × 0.70，🔴 系统风险

**实例**：新西兰 vs 埃及 DRI = 49.53 → 置信 68% × 0.85 = 58%

### 2.4 模块三：Lead-Lag Chain Detection（抢先-跟随链）

**目的**：区分「真实信号」和「噪音」——关键看谁先动、谁跟、谁不动。

```
解析 yazhi 页的 change_time 字段（精确到分钟）

链类型:
1. Pinnacle 先动 → bet365 2h 内跟进 → 澳门 4h 内跟进
   → STRONG SIGNAL，概率 +10%

2. Pinnacle 动了 → 多数机构 4h 内不跟
   → WEAK SIGNAL，忽略此次波动

3. 散户层（威廉希尔/立博）先动 → Pinnacle 长时间静态
   → NOISE，忽略

4. 三层机构同时动（1h 窗口内）
   → GENUINE EVENT，概率 +5%
```

**逻辑依据**：Pinnacle 雇佣数学博士团队做定价，价格发现能力最强。如果 Pinnacle 领涨且 bet365/澳门 先后确认 → 高度可靠。反之散户情绪驱动 → 不可持续。

### 2.5 模块四：Water Flow Analysis（水位流向）

**目的**：检测 16 家 AH 博彩公司的水位是否存在同向流动，判断是否有协调性资金入场。

```
统计 16 家 yazhi 博彩公司的水位变动方向:
  flowing_in:  home water↓（主水降）OR away water↑（客水升）= 看好主队
  flowing_out: home water↑（主水升）OR away water↓（客水降）= 看淡主队

Flow Ratio = |flowing_in − flowing_out| / 16

Flow Ratio ≥ 0.70（>11/16 同向）:
  + Pinnacle 领涨 → STRONG，概率 +8%
  + Pinnacle 静态 → SUSPICIOUS（可能操控盘），概率 −5%
  flowing_out → 概率 −8%

0.50–0.70 → 中度，不调整
< 0.50 → 分散，无信号
```

### 2.6 模块五：Exchange-Traditional Divergence（必发偏离）

**目的**：赔率是报价，成交量是真钱。必发交易所数据揭示实际资金流向 vs 赔率预期之间的差异。

```
从 touzhu 页提取必发数据:

volume_ratio = 热门方成交量 / 总成交量

volume_ratio > 0.80 AND Pinnacle 赔率静态 >4h:
  → RESISTANCE: 大量资金买入但价格不动 → 谨慎，−5%

volume_ratio > 0.80 AND Pinnacle 赔率在降:
  → BREAKOUT: 资金流向被尖峰机构确认 → +5%

volume_ratio < 0.50:
  → 市场兴趣寡淡 → 置信 × 0.90
```

**实例**：西班牙 vs 沙特，必发主胜 £117 万（86.3%），Pinnacle 主胜 1.11→1.09（继续降）→ BREAKOUT，确认为真信号。

### 2.7 模块六：Kelly Consensus（凯利共识）

**目的**：凯利指数衡量博彩公司在该赔率上的赔付压力。多家凯利同时偏高 = 机构集体承担风险。

```
从 ouzhi 页提取 30 家的凯利指数:

Avg_Kelly_favorite = 预测胜出方的凯利均值
Avg_Kelly_others = 另外两方的凯利均值

Avg_Kelly_favorite > 1.05 AND Avg_Kelly_others < 0.90:
  → VALUE SIGNAL: 机构集体看到热门方价值，概率 +5%

全部三方 Avg_Kelly < 0.90:
  → HIGH VIG: 利润率过高，信号减弱，置信 × 0.90

Avg_Kelly_favorite < 0.85:
  → OVERROUND TRAP: 庄家抽水太重，所有信号降级
```

### 2.8 四條新增 MBI 陷阱规则（#16–#19）

| # | 规则 | 触发条件 | 严重度 |
|:--:|------|---------|:---:|
| 16 | **层级分歧** | Sharp 层与 Asian 层 AH 盘口差距 ≥0.25 球 | 🔴 系统风险 |
| 17 | **必发成交量异动** | 必发成交量 > 前值 2× + 赔率静止 | ⚠️ 阻力位 |
| 18 | **凯利共识缺口** | fav Kelly >1.05 + others <0.85 | ✅ 价值信号 |
| 19 | **水位异常流动** | Flow Ratio ≥0.70 + Pinnacle 未动 | ⚠️ 操控嫌疑 |

配合原 v2.9 的 15 条欧亚陷阱 + 28 条通用规则，共计 **19 条陷阱 + 28 条通用 = 47 条规则**。

### 2.9 MBI 合成（Step 10 第 12 项校正）

```
#12 MBI Composite = SCS_adjustment + DRI_adjustment + LeadLag_adjustment

  SCS_adjustment = (SCS − 0.50) × 0.20    → 映射到 ±10%
  DRI_adjustment = (0.30 − DRI) × 0.30     → 映射到 ±10%
  LeadLag_adjustment = 根据链类型 ±5-10%

最终: base_mbi 作为概率偏移量加到预测结果上
      如果 DRI > 70 → 强制覆盖: 置信 × 0.70
```

---

## 3. 12+1 步完整分析流程

| 步骤 | 名称 | 做什么 | 数据源 |
|:--:|------|------|:---|
| 0 | 数据管道 | 检查/拉取 {date}_deep.json | scraper |
| 1 | 数据加载 | 队名校验(KB-0)，确认 6 页数据完整 | JSON |
| 1.5 | 反叙事 | 3 问：(a)「已知新闻」驱动赔率？−5%；(b) 世界杯首秀/长期缺席？+10%mot；(c) de-vig 平局 >27%？→ 完整 3-way | JSON + WebSearch |
| 2 | 基本面 | 伤停/状态/H2H，权重 v2.0(KB-5)。流动性(KB-7§14.9)。外部因素(KB-7§14.10) | shuju + WebSearch |
| 3a | **MBI 共识** | SCS + DRI + Lead-Lag + Water Flow + Exchange + Kelly → MBI 面板 | ouzhi/yazhi/touzhu |
| 3b | 1X2 数学 | Pinnacle de-vig + 层级均值 de-vig。Overround = Σ(1/odds) | ouzhi |
| 4 | 欧亚对比 | 理论 AH vs 实际 AH。15+4 条 MBI 陷阱。命中 → ±10%。≥2 命中 → 🔴 HIGH | yazhi + ouzhi |
| 5 | 开盘分析 | 开盘 vs 公允价值。深盘/浅盘/中性。三层机构对比 | yazhi + ouzhi |
| 6 | 临场走势 | 最后 6h→2h→30min。16 家 AH 水位趋势。多时段融合(KB-7§14.7) | yazhi |
| 7 | 6D 评分 | 6 维度 0–6 分。D2 现用全部 16 家 AH。D6 含离散值检查。原始 6→有效 5 | 综合 |
| 8 | 陷阱清单 | KB-2(15) + KB-3(28) + KB-10(4) = 47 条规则。≥2→🔴。≥3→「系统风险」 | 综合 |
| 9 | 总结 | 胜平负方向。红=胜/琥珀=平/绿=负。结论置顶 | — |
| 10 | 概率 + 比分 | De-vig 基础 → 11+1(MBI) 项校正 → 归一化。xG → Poisson → 前 3 比分 | KB-6, KB-7 |
| 11 | 免责声明 | 仅教育用途。xG 为市场隐含，非 Opta | — |

---

## 4. 概率合成模型

### 4.1 基础概率

```
Overround (利润率) = 1/home_price + 1/draw_price + 1/away_price
True de-vigged prob = (1/outcome_price) / overround

数据源: Pinnacle 即时 SPF
```

### 4.2 12 项校正因子（按顺序施加，最后归一化）

| # | 校正项 | 幅度 | 触发条件 |
|:--:|------|:---:|------|
| 1 | 欧亚陷阱命中 | ±10% | 15 条中任一条触发 |
| 2 | 开盘法则命中 | ±8% | 4 条开盘法则触发 |
| 3 | 临场走势真实性 | ±7% | 通过/未通过检查 |
| 4 | 赔率压缩强度 | +7/+7/+3.5/0% | 极端/强/中/弱 |
| 5 | 基本面一致性 | ±5% | 加权基本面匹配 |
| 6 | 背水一战效应 | 平+5~8%, 客+5% | 淘汰威胁队 |
| 7 | 叙事折扣 | 热门 −5% | 赔率由「已知新闻」驱动 |
| 8 | 6D 评分 | ±3% | ≥4→+3%; ≤2→−3% |
| 9 | SBO/亚洲分歧 | ±3% | 亚洲尖峰 vs Pinnacle 差距 |
| 10 | 市场流动性 | 置信 ×(0.85–1.00) | KB-8 指标 |
| 11 | 外部因素 | xG ×(1±penalty×0.03) | 天气/旅途/休息/海拔 |
| **12** | **MBI 合成** | **−10%~+10%** | **SCS + DRI + Lead-Lag** |

**归一化（强制）**：
```
raw = base + Σ(corrections_1_to_12)
normalized[i] = raw[i] / Σ(raw) × 100%
```

### 4.3 xG 推导 + Poisson 比分预测

```
1. home_xG = (OU_line / 2) + (AH_line × 0.5)
   away_xG = (OU_line / 2) − (AH_line × 0.5)

2. 施加 14.0a-d 修正（强队/弱队/东道主/淘汰赛）
3. 施加 14.1 AH 水位校准: xG ×= (1 / ah_water)
4. 施加概率调整: xG ×= (pred_prob / 50%)
5. Poisson 分布: P(n:m) = Poisson(n, home_xG) × Poisson(m, away_xG)
6. 排名 → 前 3 比分
```

---

## 5. 6D 评分模型

| 维度 | 评分标准（通过=1，不通过=0） |
|:---:|------|
| **D1 基本面** | 伤停/动力/状态/H2H 与市场方向一致 + 背水一战检查通过 + 叙事干净 |
| **D2 欧亚匹配** | **所有 16 家**AH 盘口与理论 AH 差距 ≤0.25 球（v3.0 升级：不再只看 3 家） |
| **D3 开盘客观** | 开盘符合实力对比（无人为 >0.25 球偏离）+ 无「已知新闻」压缩 |
| **D4 临场干净** | 无陷阱触发 + 无收割模式 + 最后 1h 通过真实性规则 |
| **D5 水位逻辑** | 水位变化可由基本面解释 + 平局概率 ≤25% + 水位 σ <0.06 |
| **D6 无一边倒** | 多机构一致 + 离散值 DRI < 40 + bet365-Pinnacle SPF 价差 <0.05 |

**通胀惩罚**：原始 6 分 → 有效分 = 5（6/6 历史准确率 0%，属过度自信）

**应用**：≥4 分 → 方向置信 +3%，≤2 分 → 全部降级。

---

## 6. 混合过关组合规则

### 6.1 七条铁律

| 规则 | 内容 |
|:---:|------|
| **1** | 单点故障审计：列出所有方案中作为锚点的比赛。若任一场出现在全部方案中 → 🚨 单点故障，P(全灭) ≥ P(锚失利)。修复：新增 ≥1 个不包含该锚的方案 |
| **2** | P(全灭) 必须枚举计算：不能简化为「锚失利 × 其他失利」。正确做法：枚举全部 N 场比赛的 2^N 种结果组合，累加无方案命中的组合概率 |
| **3** | 两两覆盖：当 ≥3 场高概率 SPF（P>55%），构建全部 3 对 2 串 1 作为核心。保证：任意 2 场胜出 → 至少 1 注命中 |
| **4** | 金字塔分配：最高 P_hit 的方案必须独自覆盖总预算（权重 × 赔率 ≥ 总预算）。剩余预算逐级分配。严禁等权分配 |
| **5** | JQS 精度：只选精确进球数（0/1/2/3/4/5/6/7+），不选区间。选 Poisson 最大值 |
| **6** | 输出语言：过关部分 100% 中文全称队名 |
| **7** | 盈亏范围输出：必须包含场景表 + 盈亏表 + P(全灭) + P(不亏) + EV |

### 6.2 选择与分配

```
选择标准:
  - 6D ≥ 3 分才入选
  - 平局 de-vig >27% → 跳过
  - 双方概率差 <15pp → 跳过
  - <2 场可选 → 整体跳过

分配:
  Alloc_i = P_hit,i / ΣP_hit × Budget → 取整到 ¥2 的倍数
  保守方案: Alloc × Odds ≥ Budget（覆盖本金）
  P_hit 来源: Pinnacle de-vig × Poisson，不用竞彩隐含概率

校验:
  独立锚点数 ≥ 2
  P(全灭) ≤ 30%（否则加对冲）
  保守 P_hit < 0.10 → 跳过
  激进 P_hit < 0.02 → 合并
```

### 6.3 风险日跳过规则

```
以下任一情况触发「全日跳过」:
  - ≥75% 比赛为平局结果
  - ≥2 场比赛核心热门赔率 <1.30 且失利
  - ≥2 支首秀/长期缺席球队参赛
```

---

## 7. 数据流向示意

```
500.com 6 个深度分析页
  │
  ├─ ouzhi (30家 SPF) ──────→ SCS, DRI, Kelly Consensus
  ├─ yazhi (16家 AH) ───────→ Lead-Lag, Water Flow, Tier Divergence
  ├─ touzhu (必发) ─────────→ Exchange Divergence
  ├─ shuju (基本面) ────────→ Step 2 (H2H/状态/排名)
  ├─ rangqiu (RQSPF) ───────→ 竞彩 RSPF 参考
  └─ daxiao (OU) ───────────→ xG 推导 + Poisson
  │
  ▼
  标准化 JSON
  │
  ▼
  Step 3a: MBI 六大模块并行 → MBI 面板
  Step 3b: Pinnacle de-vig → 基础概率
  Step 4-8: 欧亚陷阱 + 47条规则 + 6D评分
  Step 10: 基础概率 + 12项校正 → 归一化 → xG → Poisson → 比分
  Step 9: 方向判定 → 过关组合(Rule 1-7)
```

---

## 8. 已知局限（供专家评审时关注）

1. **xG 为市场隐含推导**：未接入 Opta/StatsBomb 真实射门数据。极端实力悬殊时比分偏差 ±1 球。
2. **30 家机构中有重复母公司**：部分博彩公司共享同一赔率源（白标），水印分析时已做聚类去重。
3. **三层权重为主观设定**：Sharp 0.55 / Asian 0.25 / Retail 0.20 基于经验，待 500+ 场回测校准。
4. **Lead-Lag 依赖时间戳精度**：500.com 的 change_time 可能存在延迟，已加入时间戳质量校验。
5. **历史回测**：v2.9 W/L 79.2%（28 场），样本太小。v3.0.1 尚未独立回测。
6. **竞彩覆水率 89%（vs 国际 97%）**：长期 EV 天然为负。
7. **分赛事建模待实施**：当前全赛事共用一套参数，联赛/杯赛/友谊赛差异未区分。

## 9. 修正日志（基于专家评审）

| 版本 | 修正项 | 类型 |
|:---|:---|:---:|
| **v3.0.2** | Kelly: 两框架分场景判读（做市 vs 博弈） | 🔴 Level 1 |
| v3.0.2 | Kelly: 返还率相对阈值替代固定绝对值 | 🔴 Level 1 |
| v3.0.2 | Kelly: 强制交叉验证（LeadLag+WaterFlow 同向才生效）| 🔴 Level 1 |
| v3.0.2 | Kelly: 成交量门槛（>£200K 启用博弈逻辑）| 🔴 Level 1 |
| v3.0.2 | 资金管理: 回撤收紧 5%/15%（原 15%/30%） | 🟢 Level 3 |
| v3.0.2 | 赛事相关性: 系数 0.25/0.55（替代 1.15 乘法因子）| 🟢 Level 3 |
| v3.0.2 | EV: 加入竞彩 0.89 返还率折扣 | 🟢 Level 3 |
| v3.0.2 | 新增 KB-10.11 数据质量校验 | 🟢 Level 3 |
| v3.0.1 | 概率校正从加法改为 logit 空间 | 🔴 Level 1 |
| v3.0.1 | 去抽水升级为 Shin 算法 | 🔴 Level 1 |
| v3.0.1 | 因子相关性 dampening | 🔴 Level 1 |
| v3.0.1 | SCS: 动态权重+幅度+时间衰减+自适应噪音 | 🟡 Level 2 |
| v3.0.1 | DRI: 联赛分位数+信息vs混乱型离散 | 🟡 Level 2 |
| v3.0.1 | Water Flow: 盘口档位控制+机构去重 | 🟡 Level 2 |
| v3.0.1 | Lead-Lag: 亚洲赛事优先+幅度加权 | 🟡 Level 2 |
| v3.0.1 | Exchange: VWAP+盈亏敞口+体量折扣 | 🟡 Level 2 |
| v3.0.1 | 6D: 二值→连续分，维度合并去重叠 | 🟡 Level 2 |
| v3.0.1 | 资金管理: 分数凯利+最大回撤+滑点 | 🟢 Level 3 |
| — | 回测框架 500+ 场 | 🔵 Pending |
| — | 分赛事建模 | 🔵 Pending |
| — | 新闻事件自动检测 | 🔵 Pending |
