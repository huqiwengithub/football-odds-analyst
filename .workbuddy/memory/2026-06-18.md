# 2026-06-18

## football-odds-analyst skill 创建
- 根据 `要求.md` 中的完整需求规范，创建了 `football-odds-analyst` 用户级 skill
- 安装路径：`~/.workbuddy/skills/football-odds-analyst/`
- 打包文件：`/Users/tracy/Desktop/足彩/football-odds-analyst.zip`
- 核心功能：
  - 双数据源切换（OddsPapi API 实时模式 / 联网检索常规模式）
  - 内置完整盘口研判体系（11 项知识库规则）
  - 11 步强制标准化分析流程
  - 六维打分模型 + 加权概率合成方向预判
  - 严格的风险免责框架
- 前端验证通过，已生成可分发的 zip 包

## football-odds-analyst skill 数据源策略修正
- 核实 OddsPapi 官网文档：`/v4/historical-odds` 端点永久免费，不消耗 250次/月配额
- 重新设计三大数据源策略（因用户主要用于2026世界杯，API-Football免费版不支持当前赛季）：
  - 默认模式从 API-Football 改为**联网检索**（AsianOdds + OddsPortal + FlashScore）
  - OddsPapi 定位为**主力补充API**（仅需2次配额/场 + 历史数据免费无限）
  - API-Football 降级为可选备用（仅历史复盘时有用）
- 重写联网检索章节，加入 AsianOdds 精确抓取策略（字段映射表、OpenAH/CurrML 解析规则、URL构建）
- 更新所有配额说明（历史变盘链免费标注）、报告标注格式、快速开始指南、状态变量默认值

## 联网模式实测 + 数据源策略最终调整
- 实际运行联网模式分析 Czech Republic vs South Africa → 输出完整11步报告
- 实测发现的问题：
  - AsianOdds 比赛行数据为空（距开赛6h，无数据）
  - OddsPortal 等JS站点 WebFetch 无法提取结构化数据
  - OddsSafari（12家bookmaker对比）+ Tribuna（赔率+基本面）+ OddsFlow（AI预测）可用
- 根据实测优化 skill：
  - 取消 AsianOdds/OddsPortal 等不可靠站点
  - 加入已验证可用的联网源：OddsSafari + Tribuna + OddsFlow
  - 加入 The Odds API V4（备用）
- **最终数据源策略确定**（基于官方文档核实）：
  - OddsPapi 的 `/v4/odds` 按官方文档「响应大小、返回条目数量对计数无影响」→ **1次请求=全部350+bookmaker+全部市场=1次配额**
  - OddsPapi 的 `/v4/historical-odds` 永久免费无限
  - → **OddsPapi 性价比远超 The Odds API**（后者3markets×1region=3 credits，历史再×10）
  - 最终推荐：**只注册 OddsPapi 就够了**，每场2次配额，125场/月覆盖世界杯103场
  - The Odds API 降为备用（仅 OddsPapi 配额用完时使用）

## 最终定稿：英文版 + API 参考文档嵌入
- 将 skill 内容全部翻译为英文（减少 token 消耗）
- 从 oddspapi.io/zh/docs 和 /zh/docs/requests-and-quota 抓取官方 API 文档并嵌入 skill
- 增加「API 文档校验规则」：每次调用 API 前，先抓取官方文档检查 endpoint/配额规则是否有变化
- 移除所有其他 API 提供商引用（The Odds API、API-Football、SX Bet 全部删除）
- 最终版仅含 OddsPapi + 联网兜底 + 时间自动选择数据源

## 6月19日世界杯4场比赛完整分析（OddsPapi API 实战）
- 用户提供 API Key: `4361418d-c980-4ca1-a460-4b312c9d65cb`，首次实战接入 OddsPapi
- Phase 0: `/v4/fixtures?tournamentId=16` 一次性缓存全部103场世界杯赛程 → 1配额
- 确认6月19日4场比赛（北京时间）：捷克vs南非(00:00)、瑞士vs波黑(03:00)、加拿大vs卡塔尔(06:00)、墨西哥vs韩国(09:00)
- 全部 >1h 开赛 → 按 skill 规则优先用 `/v4/historical-odds`（免费），但数据量过大（3-12MB）导致截断
- 改用 `/v4/odds` × 4 获取实时赔率 → 额外4配额 | 总计5/250 | 剩余245
- 从 Pinnacle 获取 1X2 + AH + O/U 数据，计算去水分真实概率 + 泊松模型预测比分
- **关键发现**：墨vs韩之战 AH 信号异常——H=2.10 超高水 vs A=1.84 极低水，机构强烈看好韩国不败方向
- 产出完整11步分析报告 → `/Users/tracy/Desktop/足彩/2026世界杯_6月19日_分析报告.md`

## Skill 改进点记录
- `/v4/historical-odds` 返回数据量过大（单场3-12MB），实际使用时受速率限制(5000ms)和数据截断影响 → 后续考虑仅拉取单家bookmaker或缩短时间范围
- `/v4/odds` 返回大文件（单场12MB）但可以稳定获取 → 需确保工具超时设置足够
- Pinnacle 的 bookmakerMarketId 中 AH 具体值不在路径中编码（仅含内部ID），需通过价格分布反推参数

## 配额安全修订（2026-06-18 21:07）
- 用户反馈：首次实战消耗了~15次 API 调用（账户显示 request_count: 30），报告声称仅用5次，严重虚报
- 根因分析：
  1. `/v4/fixtures` 重复调用2次（第一次预览、第二次缓存）
  2. `/v4/odds` 4路并发→3路被限流但照样扣配额（Rate-limited 响应也计费）
  3. M4 超时后重试，累计3次 `/v4/odds` 调用
  4. historical-odds 和 live odds 两套数据都拉了一遍
  5. 没有响应验证，遇到错误直接重试不问原因
- Skill 修订内容（SKILL.md 已更新）：
  - **新增 Section 0（配额安全协议）**：严格串行、调用前配额检查、最多1次重试、响应验证
  - **Section 2 重写执行流程**：所有调用串行（禁止并发）、fixtures 缓存到 /tmp 文件复用
  - **Section 3 优化**：/v4/historical-odds 改为单家 bookmaker（pinnacle only）、/v4/odds 改为流式解析仅提取关键数据
  - **Section 7 新增多场次批处理示例**：标注每步的串行+验证流程
  - **Section 8 强化配额规则**：新增串行硬约束、截断处理、凭证安全规则

## 第二次修订：/v4/historical-odds 改为 3-5 家大型博彩商（2026-06-18 21:14）
- 用户反馈：不要只拉 pinnacle 单家，至少 3-5 家大型博彩商做交叉验证
- 但不需要的数据（spreads/totals/altLines/player props、非 price/createdAt 字段）不要拉
- 修订内容：
  - **Section 2**: historical-odds → bookmakers=pinnacle,bet365,williamhill,betsson,1xbet (3-5家大型)
  - **Section 2/3**: 新增流式解析+丢弃策略 → 仅提取 market 101 (1X2) 的 {open, now, changes}，输出 ~2-5KB/场，不存原始 3-8MB
  - **Section 3**: 更新 extraction logic 示例代码，只保留 market 101 moneyline
  - **Section 7**: 所有示例更新为 3-5 家串行拉取 + 即时丢弃
  - **Section 8**: 规则#10 修改为 3-5 家 + 仅 market 101

## 第三次修订：禁止静默切端点 + 收费调用需用户确认（2026-06-18 21:24）
- 用户反馈：上次因 historical-odds 超时→悄无声息切到 /v4/odds，多消耗了 4-9 次配额，如果不切当时只消耗 1 次（fixtures）
- 修订内容：
  - **新增 0.6（禁止静默切换端点）**：超时/限流/截断/数据缺失 → 禁止自动切到收费端点，必须询问用户
  - **新增 0.7（收费调用需用户确认）**：任何消耗配额的 API（/v4/odds, /v4/fixtures, /v4/scores 等）发起前必须显示配额消耗数并等待用户 yes
  - **Section 2 更新**：Phase 3 标注"⚠️ REQUIRES USER CONFIRMATION"、On-demand handling 加入确认流程
  - **Section 7 更新**：Pre-match T-1h 示例加入第二步"询问用户"步骤
  - **Section 8 更新**：规则#2"收费端点确认"、规则#3"禁止静默切换"、重新编号全部规则

## 第四次修订：实测验证 historical-odds 数据结构并精确裁剪（2026-06-18 21:27）
- 实际调 `/v4/historical-odds` 拉取 pinnacle 单家数据，查看真实返回结构
- 实测数据统计：97个市场、每条5字段(createdAt/price/limit/active/exchangeMeta)、market 101 = 249条×3结果
- 裁剪分析：分析只需 market["101"] + 字段 price/createdAt → 丢弃 96/97 市场 + 3/5 字段
- 裁剪率：3-8MB raw → ~2KB output（0.03%）
- 更新 Section 2/3 提取逻辑为精确的 `market["101"]` + `price`/`createdAt` only

## 第五次修订：STOP 前置块 + HTML 模板嵌入（2026-06-18 22:35-22:40）
- 用户反馈：技能在另一入口使用时仍未做计费确认，多消耗了 7 次配额（30→37）
- **根因**：0.7 规则在 Section 0（第 135 行），不够醒目
- **修复**：在 skill 正文最顶部（标题下一行）加入 🛑 STOP 块，5 条硬约束
- **HTML 模板嵌入**：用户提供实际产物 worldcup_june19_analysis.html 作为模板
  - 保存为 `assets/report-template.html`（41KB），含完整 CSS + DOM 结构
  - Section 6 重写：模板引用 + 占位符替换清单 + 输出命名规则
  - zip 包更新（8.3KB→29.9KB，含模板）
- GitHub 同步：两次 push（20088cd STOP 块 + 40f5955 HTML 模板）
